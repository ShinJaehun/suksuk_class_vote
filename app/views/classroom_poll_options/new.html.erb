<% if turbo_frame_request? %>
  <%= turbo_frame_tag "school_poll_modal" do %>
    <div class="fixed inset-0 z-50 flex items-center justify-center overflow-y-auto bg-stone-950/40 p-4">
      <%= render "poll_definitions/option_form", poll: @poll, contest: @contest, option: @option,
                 form_url: poll_poll_session_contest_options_path(@poll, @poll_session, @contest), heading_action: "추가",
                 submit_label: "추가", cancel_path: poll_poll_session_path(@poll, @poll_session),
                 photo_management_allowed: false %>
    </div>
  <% end %>
<% else %>
  <%= render "poll_definitions/option_form", poll: @poll, contest: @contest, option: @option, form_url: poll_poll_session_contest_options_path(@poll, @poll_session, @contest), heading_action: "추가", submit_label: "추가", cancel_path: poll_poll_session_path(@poll, @poll_session), photo_management_allowed: false %>
<% end %>
