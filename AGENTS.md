# AGENTS.md

## 목적

이 문서는 `suksuk_class_vote` 저장소에서 작업하는 에이전트(Codex 등)가
프로젝트의 기본 작업 원칙과 문서 참조 순서를 일관되게 따르도록 하기 위한 안내서다.

`쑥쑥교실투표`는 교사가 교실에서 장치 하나로 학급 선거를 진행할 수 있게 하는
감독형 투표 도구다.

구체적인 제품 범위, 투표 도메인, 중단 복구, 중복 제출 방지, 집계/비밀투표 원칙,
학생 명단 bulk import 흐름은 `docs/` 아래 기준 문서를 따른다.

---

## 기본 작업 원칙

* Rails 관례(Rails way)를 최우선으로 따른다.
* 꼭 필요하지 않다면 비표준 구조, 과한 추상화, 과한 메타프로그래밍을 피한다.
* 기존 public behavior를 함부로 바꾸지 않는다.
* 작은 단위로 읽고, 작은 단위로 수정하고, 변경 이유를 분명히 남긴다.
* 새로운 코드는 현재 코드베이스의 구조와 스타일에 최대한 맞춘다.
* UI 문구나 마크업만 보고 성급히 수정하지 말고, 관련 도메인/흐름/정책 문서부터 확인한다.
* view에는 가능한 한 로직을 최소화하고, 권한/분기/도메인 판단은 controller, policy, model, helper, service 등 적절한 계층에 둔다.
* 권한 판단은 view에서 직접 해결하지 말고, controller와 policy를 기준으로 읽기 쉽게 유지한다.
* 문자열이 새로 추가되거나 변경될 때는 처음부터 `locales` 반영 여부를 함께 고려한다.
* 선거/투표 관련 기능은 편의성보다 상태 무결성, 중복 방지, 복구 가능성을 우선한다.
* 기능을 추가하기 전에 관련 문서가 있는지 먼저 확인하고, 문서가 없다면 구현보다 문서 초안을 우선 제안한다.

---

## 문서 우선순위

작업 전 아래 문서를 가능한 먼저 확인한다.

1. `AGENTS.md`

2. 현재 시스템/아키텍처 문서

   * `docs/architecture/*.md`

3. 현재 작업과 직접 관련된 spec 문서

   * `docs/specs/*.md`

4. 테스트 전략 문서

   * `docs/testing/*.md`

5. 운영 문서

   * `docs/ops/*.md`

원칙:

* 관련 문서가 없거나 부족하면, 바로 구현에 들어가지 말고 먼저 문서 초안을 제안한다.
* 문서가 현재 구현과 다르면, 구현 기준으로 무리하게 추측하지 말고 사용자에게 확인한다.
* `docs/archive/*`, `docs/legacy/*`는 사용자가 요청하거나 현재 문서만으로 맥락을 알 수 없을 때만 참고한다.
* spec만 보지 말고, 해당 spec의 판단 근거가 되는 정책/구조 문서가 있으면 함께 확인한다.
* 코드 변경으로 현재 구현/정책/남은 작업이 달라지면 관련 기준 문서를 함께 갱신한다.
* 관련 없는 다른 문서까지 함께 수정하려고 하지 않는다.

---

## 권한/역할 원칙

초기 역할은 `admin`, `teacher`를 중심으로 단순하게 유지한다.

* `admin`

  * 서비스 전체 최고 관리자다.
  * 교사 계정, 학급, 투표자 그룹, 선거, 후보, 투표소, 결과 등 전체 데이터를 관리할 수 있다.
  * 특정 선거 유형에만 묶인 역할이 아니라 운영상 필요한 모든 권한을 가진다.
  * 위험한 관리 기능일수록 감사 로그와 확인 절차를 둔다.

* `teacher`

  * 본인 계정으로 로그인한다.
  * 본인이 관리하는 투표자 그룹, 선거, 후보, 투표 진행, 결과 확인을 담당한다.
  * 다른 교사의 투표자 그룹, 선거, 결과에는 접근하지 못한다.

* `student`

  * 초기 MVP에서는 로그인 계정으로 두지 않는다.
  * 학생은 개인 계정, PIN, 개인 단말 없이 교사 장치 앞에서 투표한다.
  * 시스템 안에서 학생은 인증 주체가 아니라 투표자 그룹의 명단 데이터로 취급한다.

권한 판단은 controller/policy 중심으로 유지한다. view에서 직접 복잡한 권한 조건을 늘리지 않는다.

---

## 아키텍처/구현 원칙

* 새 기능을 추가할 때는 먼저 현재 구조와 naming, partial 분리 방식, controller 책임 범위를 확인하고 그 흐름을 따른다.
* 불필요한 새 객체, 새 패턴, 새 계층을 성급히 늘리지 않는다.
* controller는 인증/권한/흐름 제어를 담당하되, 과도한 도메인 로직을 밀어 넣지 않는다.
* model에는 데이터 불변식과 단순 도메인 규칙을 둔다.
* 상태 전이가 중요한 작업은 service object를 사용할 수 있다.
* service object를 만들 경우 이름과 책임을 작게 유지한다.
* helper는 view 표현을 단순하게 만들기 위한 용도로 사용한다.
* view에는 가능한 한 계산/권한/도메인 판단을 직접 넣지 않는다.
* 기존 route, controller, view 구조가 있다면 먼저 그 흐름을 따른다.
* 새 구조가 필요하면 Rails 관례에 맞는 가장 단순한 형태부터 제안한다.
* Turbo 응답과 HTML 응답은 둘 다 깨지지 않도록 주의한다.
* 실시간 기능은 초기 MVP의 핵심이 아니므로 ActionCable을 성급히 도입하지 않는다.

---

## 변경 승인 원칙

* 기본적으로 파일을 바로 수정하지 말고, 먼저 변경 목적, 수정 대상 파일 목록, 핵심 변경 요약, 위험/주의점을 제시한다.
* 사용자가 승인하기 전에는 실제 파일 수정(write)을 하지 않는다.
* 사용자가 승인한 뒤에는 별도 요청이 없는 한 전체 diff를 다시 제시하지 말고 작업을 진행한다.
* 전체 diff나 unified diff는 사용자가 명시적으로 요청한 경우에만 제시한다.
* 문서 작업도 동일하게 적용한다.
* 사용자가 “바로 반영해도 된다”고 명시한 경우에만 즉시 수정한다.
* 변경 범위가 클 경우, 파일별 수정 계획을 먼저 요약한다.
* 작업 도중 예상보다 범위가 커지면 멈추고 사용자에게 범위 재확인을 요청한다.

---

## spec 기반 작업 원칙

* 작업은 `docs/specs/*.md`를 기준으로 진행한다.
* 새로운 기능이나 리팩토링 단위가 필요하면 먼저 해당 spec 초안을 제안한다.
* 관련 spec이 아직 없다면, 바로 구현에 들어가지 말고 먼저 spec 초안을 제안한다.
* spec 변경이 필요하면 먼저 제안하고 승인 후 반영한다.
* 임의로 spec 밖의 기능을 추가하지 않는다.
* 별도 지시가 없으면, 구현과 함께 관련 문서도 같이 갱신한다.
* 코드 변경으로 문서화된 현재 구현/정책/남은 작업이 달라지는 경우, 해당 기준 문서를 함께 갱신한다.
* 관련 없는 다른 문서까지 함께 수정하려고 하지 않는다.
* 새 기능이 기존 spec의 기준을 대체하면 active spec을 둘로 나누어 병렬 유지하지 않는다.
* canonical spec을 하나로 정하고, 중복되는 기존 spec은 필요한 내용을 흡수한 뒤 삭제하거나 archive로 이동한다.

---

## 토큰 절약 작업 원칙

* 기본적으로 항상 토큰 절약 모드로 작업한다.
* 작업 전에는 먼저 `git status --short`만 확인한다.
* 관련 파일만 읽고, 관련 파일만 수정한다.
* 파일 전체 출력은 피하고, 필요한 경우 관련 메서드, partial, spec 주변만 읽는다.
* 파일을 읽을 때는 먼저 필요한 줄 범위를 좁히고, 한 번에 긴 구간을 출력하지 않는다.
* 전체 코드베이스 검색은 꼭 필요할 때만 한다.
* 검색이 필요하면 넓은 키워드보다 controller/action/model/spec 이름 등 좁은 단서부터 사용한다.
* 이미 사용자가 제공했거나 직전 작업에서 확인한 구조를 반복해서 읽지 않는다.
* 사용자가 붙여넣은 로그, diff, 문서 내용은 다시 조회하거나 재출력하지 말고 필요한 결론만 참조한다.
* 전체 `git diff`, unified diff, 테스트 로그, route 전체 목록, 긴 backtrace를 출력하지 않는다.
* 명령 출력은 가능한 `--short`, `--name-only`, `--stat`, `sed -n` 등으로 제한하고, 최종 답변에는 핵심만 요약한다.
* 사용자가 명시적으로 요청하지 않는 한 테스트를 직접 실행하지 않는다.
  * `bundle exec rspec` 실행 금지
  * `bin/rails test` 실행 금지
  * `bin/rails routes` 실행 금지
  * `git diff --check` 실행 금지
* 테스트, route 확인, diff check는 사용자가 직접 실행하는 것을 기본으로 한다.
* Codex는 작업 완료 후 사용자가 실행할 확인 명령만 짧게 제시한다.
* 변경 대상 파일을 1~3개 수준으로 제한하는 것을 우선한다.
* 작업 범위가 3개 파일을 넘거나 새 route/model/service/migration이 필요해지면, 수정 전에 멈추고 사용자에게 범위 재확인을 요청한다.
* 사용자가 명시적으로 요청하지 않는 한 문서, 테스트, 구현을 한 번에 모두 수정하지 않는다.
* 긴 계획을 쓰지 않는다. 필요한 경우 수정 대상과 목적만 3줄 이내로 말한다.
* 제안 단계에서는 대안 나열을 최소화하고, 권장안 1개와 필요한 경우 위험 1개만 짧게 제시한다.
* 작업 완료 보고는 특별한 사유가 없으면 15줄 이내로 제한한다.
* 작업 완료 보고는 아래 형식을 따른다.

```text
수정 파일:
- ...

핵심 변경:
- ...
- ...
- ...

건드리지 않음:
- ...

사용자 실행:
- ...

커밋하지 않음.
```

* “Explored”, “Ran”, “Edited” 식의 장황한 작업 로그를 본문에 반복하지 않는다.
* 실패나 불확실한 부분을 발견하면 임의로 계속 수정하지 말고, 원인 후보와 확인 명령만 3줄 이내로 제시한다.
* 전체 테스트, 전체 diff 확인, 커밋, push, merge는 기본적으로 사용자가 직접 수행한다.
* 사용자가 명시적으로 요청하지 않는 한 Codex가 직접 커밋하지 않는다.
* `git add .`는 사용하지 않는다.
* 커밋이 필요한 경우에도 사용자가 명시한 파일만 `git add <file>` 형식으로 스테이징한다.
* 한 세션에서는 가능하면 하나의 기능이나 하나의 작은 문서 정리만 처리한다.

---

## Codex 응답 형식 원칙

* Codex는 작업 중 장황한 진행 로그를 쓰지 않는다.
* 중간 보고가 필요한 경우 한 문장으로 제한한다.
* 최종 보고는 “수정 파일 / 핵심 변경 / 사용자 실행 명령 / 커밋 여부” 중심으로 짧게 작성한다.
* 사용자가 요청하지 않은 분석 보고서, 긴 회고, 전체 diff 설명을 출력하지 않는다.
* 사용자가 “분석만” 요청한 경우 파일을 수정하지 않는다.
* 사용자가 “수정만” 요청한 경우 불필요한 설계 설명을 늘리지 않는다.
* 사용자가 “테스트는 내가 실행”한다고 한 경우 테스트 명령을 실행하지 않고 제시만 한다.
* 사용자가 실패 로그를 붙여넣으면 전체 로그를 다시 반복하지 말고, 실패 원인 후보와 다음 수정 지점만 짧게 말한다.

---

## 브랜치 운영 원칙

* 브랜치는 너무 잘게 쪼개지 않는다.
* 하나의 기능 흐름이나 정책 검토 흐름이 이어지는 동안에는 같은 의미 있는 브랜치에서 작업을 계속한다.
* 브랜치는 “작업 단위”가 아니라 “의미 있는 변경 묶음” 기준으로 만든다.
* 단, `main`에는 직접 작업하지 않는다.
* 이미 관련 브랜치가 열려 있다면, 새 브랜치를 만들기보다 그 브랜치에서 이어서 작업할 수 있는지 먼저 확인한다.
* 브랜치 분리가 필요한 경우는 기능 방향이 명확히 달라지거나, 위험도가 높은 변경을 기존 작업과 분리해야 할 때로 제한한다.
* 초기 문서/기반 정리 브랜치는 `docs/project-foundation`을 권장한다.

---

## 정리/복구 원칙

* 구현 방향이 바뀌어 더 이상 필요 없어졌다면, 관련 파일과 코드도 함께 정리 대상으로 본다.
* 새 구조를 넣으면서 불필요해진 controller, view, helper, route, 테스트, 문서가 남지 않도록 정리한다.
* 무언가를 추가했다가 방향을 바꿔 되돌리는 경우, 남겨둘 이유가 분명하지 않다면 최대한 작업 전 상태에 가깝게 복구한다.
* 다만 사용자가 직접 만든 변경이나, 현재 작업과 무관한 기존 변경은 임의로 되돌리지 않는다.
* 삭제/복구가 필요한 파일이 있다면, 반영 전에 정리 대상 파일 목록을 먼저 요약한다.

---

## 테스트 원칙

* 테스트의 목적은 coverage 수치가 아니라 confidence 확보이다.
* 테스트 전략은 `docs/testing/rspec_strategy.md`를 따른다.
* 핵심 도메인 규칙, 권한, 상태 전이, 멱등성, request 흐름을 우선 테스트한다.
* brittle한 HTML 구조 테스트는 지양한다.
* system spec은 핵심 happy path와 실제 교실 운영 흐름 중심으로 최소화한다.
* RSpec 스타일은 readable > clever 원칙을 따른다.
* 과도한 shared context, helper, abstraction을 피한다.
* 기존 테스트와 중복되는 저가치 테스트를 늘리지 않는다.
* 전체 테스트는 사용자가 직접 실행하는 것을 기본으로 한다.
* Codex는 기본적으로 테스트를 직접 실행하지 않는다.
* Codex는 수정 후 실행할 테스트 명령만 제시한다.
* 사용자가 테스트 결과를 붙여넣으면, 그 결과를 바탕으로 다음 수정을 판단한다.
* 사용자가 명시적으로 “테스트 실행해도 된다”고 승인한 경우에만 targeted spec을 실행할 수 있다.
* 승인 없이 `bundle exec rspec`, `bin/rails test`, `bin/rails routes`, `git diff --check`를 실행하지 않는다.
* 실패 로그가 긴 경우, Codex는 전체 로그 재출력을 피하고 실패한 example, 에러 메시지, 관련 파일/라인만 요약한다.
* 테스트 성공 시에도 전체 로그를 반복하지 말고 성공한 명령과 example 수만 짧게 기록한다.

---

## 커밋 원칙

* 커밋은 기본적으로 사용자가 직접 수행한다.
* Codex는 사용자가 명시적으로 요청한 경우에만 커밋한다.
* Codex가 커밋을 수행해야 하는 경우, 먼저 커밋 대상 파일 목록을 짧게 제시한다.
* `git add .`는 사용하지 않고, 필요한 파일만 명시적으로 스테이징한다.
* 커밋 메시지는 가능하면 간결하게 작성한다.
* 커밋 메시지에는 변경 의도와 검증 방법이 드러나면 충분하다.
* 과도하게 긴 커밋 메시지를 기본값으로 요구하지 않는다.
* commit message는 한국어 우선으로 작성한다.

---

## 작업 완료 후 기대사항

* 변경 이유를 짧게 설명할 수 있어야 한다.
* 가능하면 관련 테스트를 함께 추가/수정한다.
* 새 규칙이 생겼다면 관련 문서 반영 여부를 검토한다.
* 변경 범위가 크면 후속 작업 포인트를 짧게 남긴다.
* 사용자가 실행할 확인 명령이나 테스트 명령을 짧게 제시한다.
